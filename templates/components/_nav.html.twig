<turbo-frame id="navigation">

    <!-- Top Section: Language Switcher & Portfolio Name -->
    <div class="flex flex-col md:flex-row w-full md:h-[100px] bg-white">
        <!-- Language Switcher (1/3 width) -->
        <div class="w-full md:w-1/3 flex justify-center md:justify-start items-center px-6 py-4">
{#            TODO#}
{#            <a href="#" class="transform transition-all duration-300 hover:scale-110 hover:shadow-lg">#}
{#                <img src="{{ asset('images/lang/flag-dk.svg') }}" alt="Danish" class="w-8 h-8">#}
{#            </a>#}
{#            <a href="#" class="ml-4 transform transition-all duration-300 hover:scale-110 hover:shadow-lg">#}
{#                <img src="{{ asset('images/lang/flag-en.svg') }}" alt="English" class="w-8 h-8">#}
{#            </a>#}
        </div>

        <!-- Portfolio Name (2/3 width) -->
        <div class="w-full md:w-2/3 flex justify-center md:justify-end text-4xl font-patrick-hand text-yellow-600 py-4 pr-6 animate-fade-in">
            Martins Portefølje
        </div>
    </div>

    <!-- Navigation -->
    <nav data-controller="menu" class="relative flex flex-col md:flex-row h-auto md:h-[300px] bg-white">

        <!-- Left: Logo (Now Taking md:w-1/3) -->
        <div class="relative w-full md:w-1/3 flex items-center justify-center overflow-hidden">
            <div class="relative w-96 h-96 p-2 bg-white animate-fade-in mx-auto">
                <img src="{{ asset('images/file.svg') }}" alt="Profile Image" class="w-full h-full object-contain"/>
            </div>
        </div>

        <!-- Mobile Menu Button (Centered) -->
        <div class="w-full flex justify-center py-4 md:hidden">
            <button data-action="click->menu#toggle" class="text-yellow-600 text-3xl transition-all">
                <span id="menu-icon">
                    <svg class="w-8 h-8 text-yellow-600" id="menu-open-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"/>
                    </svg>
                    <svg class="w-8 h-8 text-yellow-600 hidden" id="menu-close-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                    </svg>
                </span>
            </button>
        </div>

        <!-- Right: Menu Section (Collapsible on Small Screens) -->
        <div data-menu-target="menu"
             class="w-full md:w-2/3 bg-yellow-500 clip-triangle flex flex-col justify-center items-center text-white text-2xl font-patrick-hand animate-slide-in hidden md:flex">
            <ul class="flex flex-col space-y-3 text-center">
                <li><a href="#about" class="hover:text-yellow-300 font-semibold">Om mig</a></li>
                <li><a href="#more-about" class="hover:text-yellow-300 font-semibold">Mere om mig</a></li>
                <li><a href="#projects" class="hover:text-yellow-300 font-semibold">Mine Projekter</a></li>
                <li><a href="#services" class="hover:text-yellow-300 font-semibold">Mine Services</a></li>
                <li><a href="#contact" class="hover:text-yellow-300 font-semibold">Kontakt</a></li>
            </ul>
        </div>

    </nav>
</turbo-frame>
