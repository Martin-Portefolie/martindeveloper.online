<section id="projects" class="mt-40 py-24 bg-white text-yellow-900 eb-garamond-regular px-6 text-base leading-relaxed">
    <div class="max-w-6xl mx-auto space-y-12">

        <!-- Section Heading -->
        <div class="space-y-4">
            <div class="w-20 border-t-8 border-yellow-500"></div>
            <h2 class="text-4xl font-bold text-yellow-600">Mine Projekter</h2>
        </div>

        <!-- Project Grid -->
        <div class="grid sm:grid-cols-1 lg:grid-cols-2 gap-8">



            <!-- Project Card 1 -->
            <div class="bg-yellow-50 border border-yellow-500 ring-2 ring-yellow-700 rounded-2xl shadow-lg overflow-hidden flex flex-col transition duration-300 hover:ring-yellow-600 group">
                <!-- Image -->
                <div class="overflow-hidden">
                    <img src="{{ asset('images/hest-test.png') }}"
                         alt="Arkitekt Site"
                         class="w-full h-56 object-cover transition-transform duration-300 group-hover:scale-105" />
                </div>

                <!-- Content -->
                <div class="p-6 space-y-3 flex flex-col flex-grow justify-between">
                    <!-- Title + Type -->
                    <div class="space-y-1">
                        <h3 class="text-2xl font-bold text-yellow-700">Hest Test Calender Site</h3>
                        <p class="text-xs font-semibold text-yellow-800 uppercase tracking-wider">Eksamensopgave</p>
                    </div>

                    <!-- Description -->
                    <p class="text-yellow-900 text-[1.05rem]">
                        Et effektivt tidsregistreringssystem med klare roller: Admins styrer klienter, opgaver og todo’s – brugere registrerer tid og holder styr på deres egne opgaver.
                    </p>


                    <!-- Call to action -->
                    <div>
                        <a href="http://hest-test-calender.site/en/" target="_blank"
                           class="inline-block text-yellow-800 font-medium underline underline-offset-2 hover:text-yellow-600 transition">
                            Gå til projekt →
                        </a>
                    </div>
                </div>

                <!-- Technology Icons -->
                <div class="bg-yellow-100 px-4 py-3 flex flex-wrap items-center gap-2">
                    {% set techIcons = [
                        { name: 'mdi:docker', label: 'Docker' },
                        { name: 'simple-icons:nginx', label: 'Nginx' },
                        { name: 'mdi:symfony', label: 'Symfony' },
                        { name: 'simple-icons:mariadb', label: 'MariaDB' },
                        { name: 'file-icons:twig', label: 'Twig' },
                        { name: 'simple-icons:stimulus', label: 'Stimulus' },
                        { name: 'simple-icons:turbo', label: 'Turbo' }
                    ] %}

                    {% for icon in techIcons %}
                        <div class="bg-yellow-700 p-1.5 rounded-md flex items-center justify-center w-8 h-8 shadow hover:scale-105 transition-transform" title="{{ icon.label }}">
                            {{ ux_icon(icon.name, { class: 'w-4 h-4 text-white' }) }}
                        </div>
                    {% endfor %}
                </div>
            </div>


            <!-- Project Card 2 -->
            <div class="bg-yellow-50 border border-yellow-500 ring-2 ring-yellow-700 rounded-2xl shadow-lg overflow-hidden flex flex-col transition duration-300 hover:ring-yellow-600 group">
                <!-- Image -->
                <div class="overflow-hidden">
                    <img src="{{ asset('images/architect-site.png') }}"
                         alt="Arkitekt Site"
                         class="w-full h-56 object-cover transition-transform duration-300 group-hover:scale-105" />
                </div>

                <!-- Content -->
                <div class="p-6 space-y-5 flex flex-col flex-grow justify-between">
                    <!-- Title + Type -->
                    <div class="space-y-1">
                        <h3 class="text-2xl font-bold text-yellow-700">Arkitekt Site</h3>
                        <p class="text-xs font-semibold text-yellow-800 uppercase tracking-wider">Eksamensopgave</p>
                    </div>

                    <!-- Description -->
                    <p class="text-yellow-900 text-[1.05rem]">
                        Platform til arkitekter med stilrent design, glatte animationer, opgavestyring og nyhedstilmelding – alt samlet i én intuitiv løsning.
                    </p>

                    <!-- Call to action -->
                    <div>
                        <a href="http://hest-test-calender.site/en/" target="_blank"
                           class="inline-block text-yellow-800 font-medium underline underline-offset-2 hover:text-yellow-600 transition">
                            Gå til projekt →
                        </a>
                    </div>
                </div>

                <!-- Technology Icons -->
                <div class="bg-yellow-100 px-4 py-3 flex flex-wrap items-center gap-2">
                    {% set techIcons = [
                        { name: 'mdi:docker', label: 'Docker' },
                        { name: 'simple-icons:nginx', label: 'Nginx' },
                        { name: 'mdi:symfony', label: 'Symfony' },
                        { name: 'simple-icons:mariadb', label: 'MariaDB' },
                        { name: 'file-icons:twig', label: 'Twig' },
                        { name: 'simple-icons:stimulus', label: 'Stimulus' },
                        { name: 'simple-icons:turbo', label: 'Turbo' }
                    ] %}

                    {% for icon in techIcons %}
                        <div class="bg-yellow-700 p-1.5 rounded-md flex items-center justify-center w-8 h-8 shadow hover:scale-105 transition-transform" title="{{ icon.label }}">
                            {{ ux_icon(icon.name, { class: 'w-4 h-4 text-white' }) }}
                        </div>
                    {% endfor %}
                </div>
            </div>



        </div>
    </div>
</section>
